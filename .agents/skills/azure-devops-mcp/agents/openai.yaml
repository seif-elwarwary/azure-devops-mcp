interface:
  display_name: "Azure Devops Mcp"
  short_description: "Repo-specific patterns and workflows for azure-devops-mcp"
  default_prompt: "Use the azure-devops-mcp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true